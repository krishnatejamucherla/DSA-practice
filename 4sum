#include <bits/stdc++.h>
using namespace std;
vector<vector<int>> fourSum(vector<int>& nums, int target) {
        vector<vector<int>>res;
        sort(nums.begin(),nums.end());
        int i,n=nums.size();
        for(i=0;i<n;i++){
            if(i>0 && nums[i]==nums[i-1])
            continue;
            for(int j=i+1;j<n;j++){
                if(j>i+1 && nums[j]==nums[j-1])
                continue;
                int k=j+1;
                int l=n-1;
                while(k<l){
                    long long sum=(long long)nums[i]+nums[j]+nums[k]+nums[l];//we used long long because when large numbers are added it is out of integer range
                    if(sum<target){
                        k++;
                    }
                    else if(sum>target){
                        l--;
                    }
                    else{
                        vector<int>temp={nums[i],nums[j],nums[k],nums[l]};
                        res.push_back(temp);
                        k++;
                        l--;
                        while(k<l && nums[k]==nums[k-1]) k++;
                        while(k<l && nums[l]==nums[l+1]) l--;
                    }
                }
            }
        }
        return res;
    }
int main() {
    int n, target;
    cin >> n;
    vector<int> nums(n); //here if input number is >10^12 the integer doesnt take because integer size is less than that if you want that numbers also use long long
    for (auto &it : nums) {
        cin >> it;
    }
    cin >> target;
    vector<vector<int>> res = fourSum(nums, target);
    if (res.empty()) {
        cout << "No such elements found" << endl;
    } else {
        cout << "Quadruplets are: " << endl;
        for (const auto &quad : res) {
            for (const auto &num : quad) {
                cout << num << " ";
            }
            cout << endl;
        }
    }
    return 0;
}
